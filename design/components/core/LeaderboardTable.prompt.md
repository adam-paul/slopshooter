The ranked-taggers table: mono numerals, verdict-split bar per row, hairline rules only, row hover bg.

```jsx
<LeaderboardTable rows={[{ rank: 4, handle: "botspotter", checks: 45, ai: 31, mixed: 8, human: 6, score: ".78", lastAt: "1d ago" }]} />
```